# Features Directory

 This directory contains all the features of the application. Each feature should have its own folder with the following structure:

 ```
 feature_name/
   data/
     datasources/
     models/
     repositories/
   domain/
     entities/
     repositories/
     usecases/
   presentation/
     bloc/
     pages/
     widgets/
 ```
